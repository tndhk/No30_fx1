# FX自動売買システム 要件定義書 (Draft v1.0)

**プロジェクト名:** Project Quant-FX (仮)
**作成日:** 2025年11月29日
**対象:** OANDA Japan (v20 API)
**開発言語:** Python

## 1. プロジェクトの目的と範囲
* **目的:** Pythonによるクオンツ分析を用いた自動売買システムを構築し、長期的にプラスの収益を目指す。
* **運用資金:** 100万円（初期はデモおよび少額から開始）。
* **アプローチ:** まずはルールベース（テクニカル指標）で基盤を作り、将来的に機械学習モデル（ML）へ拡張可能なアーキテクチャとする。

---

## 2. システム構成 (Architecture)

### 2.1 推奨技術スタック
| カテゴリ | 推奨技術 | 理由・選定基準 |
| :--- | :--- | :--- |
| **言語** | Python 3.10+ | クオンツ業界標準。ライブラリが豊富。 |
| **API** | OANDA API (v20) | `oandapyV20` ライブラリ使用。約定力と情報の透明性が高い。 |
| **DB** | SQLite (初期) → PostgreSQL (将来) | 初期はファイルベースのSQLiteで十分。データ量が増えたら移行。 |
| **サーバー** | クラウドVPS | AWS Lightsail / ConoHa VPS (Linux Ubuntu)。24時間稼働必須。 |
| **通知** | Discord Webhook / LINE Notify | エラー時や約定時にスマホへ即時通知を送るため。 |

---

## 3. 機能要件 (Functional Requirements)

システムが「具体的に何をするか」です。以下の4つのモジュールに分割して開発することを定義します。

### 3.1 データ収集モジュール (Data Ingestion)
* **機能:** OANDAサーバーから指定した通貨ペア（USD/JPY等）のローソク足データを取得する。
* **要件:**
    * 1分足、1時間足などを定期的に取得し、データベースに保存する。
    * API制限（OANDAは秒間リクエスト数に制限あり）を超えないよう制御する。

### 3.2 戦略判定モジュール (Strategy Engine)
* **機能:** 取得したデータに基づき「売買シグナル」を生成する。
* **要件:**
    * **初期ロジック:** 移動平均線クロス、RSIなどの単純なテクニカル指標で判定。
    * **拡張性:** ロジック部分を関数化し、将来的にMLモデル（`.pkl`ファイル等）と差し替え可能にする構造とする。

### 3.3 注文執行モジュール (Order Execution)
* **機能:** シグナルに基づき、実際にAPI経由で注文を出す。
* **要件:**
    * **成行/指値:** 基本は成行（Market Order）を使用。
    * **TP/SL同時発注:** 注文と同時に必ず「利確(Take Profit)」と「損切り(Stop Loss)」を入れる（**必須安全策**）。
    * **ポジション管理:** 既にポジションを持っている場合は、追加エントリーしない（またはナンピンを許可するか設定可能にする）。

### 3.4 監視・通知モジュール (Monitoring)
* **機能:** システムの生存確認と取引報告。
* **要件:**
    * **定期生存報告:** 1時間に1回「稼働中」と通知する。
    * **約定通知:** エントリー、決済時に価格と損益を通知する。
    * **エラー通知:** 例外発生時（API接続切れなど）に即時通知する。

---

## 4. 非機能要件 (Non-Functional Requirements)

機能以外の「品質」に関わる部分です。ここが**実弾運用の生命線**です。

### 4.1 可用性・堅牢性
* **例外処理:** ネットワーク切断時、APIエラー時にシステムを落とさず、一定時間待機して再接続（リトライ）する仕組みを入れる。
* **キルスイッチ:** 緊急時に外部（スマホ等）から**全ポジションを決済してシステムを停止させる機能**、または手順を用意する。

### 4.2 セキュリティ
* **APIトークン管理:** ソースコード内にAPIキーを直接書かない（ハードコーディング禁止）。`.env`ファイルまたは環境変数で管理する。
* **ログ管理:** エラーログをファイルに出力し、原因究明できるようにする。

### 4.3 パフォーマンス
* **資金管理 (Money Management):**
    * 1回のトレードでの最大許容損失額を「資金の1%〜2%」に制限するロジックをコード内で強制する。
    * 100万円の場合、損切り幅が1万円を超えるようなロット数での注文をシステム側で拒否する。

---

## 5. 開発ロードマップ (MVPアプローチ)

いきなり完成形を目指さず、段階的にリリースします。

* **Phase 1: データ収集と保存 (1週間)**
    * OANDAに接続し、ヒストリカルデータをCSV/DBに保存するスクリプト完成。
* **Phase 2: バックテスト環境 (1〜2週間)**
    * 過去データに対して単純なロジックを適用し、勝敗をシミュレーションする。
* **Phase 3: 通信テスト・デモ運用 (1〜2週間)**
    * OANDAデモ口座で、実際に注文が通るか、通知が来るかを確認する。
* **Phase 4: 実弾運用 (本番)**
    * 最小単位（1〜100通貨）で稼働開始。

---